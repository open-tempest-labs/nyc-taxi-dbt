name: 'zea_export'
version: '1.0.0'
config-version: 2

profile: 'zea_local'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

on-run-start:
  - "{{ stage_zea_sources() }}"

models:
  zea_export:
    materialized: table
